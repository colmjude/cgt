var assert = require('assert');

describe('Routing', function() {
  describe('Fake', function() {
    it('should just work for now', function() {
         assert.equal(true, true, "True dat!");
    });
  });
});
